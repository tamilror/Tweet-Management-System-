<%= form_with(model: tweet_management, local: true) do |form| %>
  <% if tweet_management.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet_management.errors.count, "error") %> prohibited this tweet_management from being saved:</h2>

      <ul>
      <% tweet_management.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
